<?php declare(strict_types=1); require __DIR__ . '/lib/landlord.php'; handleLandlordPost('documents.php'); extract(landlordData()); renderLandlordStart('documents', 'Documents', 'Property records', 'Keep a register of the documents held for each property.', '<div class="top-actions"><button class="button primary" data-open="document-dialog">Add document</button></div>'); ?>
<section class="panel table-wrap"><table><thead><tr><th>Document</th><th>Category</th><th>Property</th><th>Notes</th><th>Added</th></tr></thead><tbody><?php foreach($documents as $document): ?><tr><td><?= e($document['name']) ?></td><td><span class="badge due"><?= e($document['category']) ?></span></td><td><?= e($document['address']) ?></td><td><?= e($document['notes']) ?></td><td><?= e(date('j M Y',strtotime($document['created_at']))) ?></td></tr><?php endforeach; ?></tbody></table></section>
<dialog id="document-dialog"><form method="post"><button type="button" class="dialog-close" data-close>&times;</button><p class="eyebrow">Records</p><h2>Add document record</h2><input type="hidden" name="csrf" value="<?= e(csrfToken()) ?>"><input type="hidden" name="action" value="add_document"><label>Property<select name="property_id"><?php foreach($properties as $property): ?><option value="<?= (int)$property['id'] ?>"><?= e($property['address']) ?></option><?php endforeach; ?></select></label><label>Document name<input name="name" required></label><label>Category<select name="category"><option>Safety</option><option>Tenancy</option><option>Finance</option><option>General</option></select></label><label>Notes<textarea name="notes" rows="4"></textarea></label><button class="button primary">Add record</button></form></dialog>
<?php renderLandlordEnd($properties); ?>
